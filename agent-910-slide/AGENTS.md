# Agent-910 Slide Workspace

This is an open-slide workspace.

- Slides live under `slides/<kebab-case-id>/`.
- Each deck entry is `slides/<id>/index.tsx`.
- The default export must be an array of open-slide `Page` components.
- Each page should fill the fixed 1920 x 1080 canvas.
- Do not add dependencies unless the presenter explicitly asks.
